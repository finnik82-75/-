# Desktop Приложение - Мониторинг конкурентов

Графический интерфейс для анализа конкурентов на базе PyQt6.

## 🚀 Быстрый старт

### Вариант 1: Запуск из исходников

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите backend сервер (в корне проекта)
python run.py

# 3. Запустите desktop приложение (в папке desktop)
python main.py
```

### Вариант 2: Использование собранного .exe

```bash
# 1. Соберите .exe файл
python build.py

# 2. Запустите backend сервер (в корне проекта)
python run.py

# 3. Запустите CompetitorMonitor.exe из папки dist/
```

## 📋 Требования

- Python 3.9+ (для запуска из исходников)
- PyQt6 >= 6.6.0
- Запущенный backend сервер на http://localhost:8000

## 🛠️ Сборка .exe

```bash
# Сборка
python build.py

# Очистка артефактов
python build.py clean
```

Собранный файл будет в `dist/CompetitorMonitor.exe` (размер ~8-9 MB)

## ✨ Возможности

- ✅ Анализ текста конкурентов
- ✅ Анализ изображений (drag & drop)
- ✅ Парсинг сайтов по URL
- ✅ Просмотр истории запросов
- ✅ Индикатор подключения к серверу
- ✅ Темная тема интерфейса

## ⚠️ Примечания

- Desktop приложение требует запущенный backend сервер
- При первом запуске может потребоваться установка ChromeDriver (автоматически)
- Для парсинга сайтов требуется Chrome браузер
